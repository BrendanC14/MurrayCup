<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.CutlerDevelopment.murraycup">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".TeamAdminMenu"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_team_admin_menu"
            android:parentActivityName=".AdminMenu"
            android:theme="@style/FullscreenTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.CutlerDevelopment.murraycup.AdminMenu" />
        </activity>
        <activity
            android:name=".AdminMenu"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_admin_menu"
            android:parentActivityName=".MainMenu"
            android:theme="@style/FullscreenTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.CutlerDevelopment.murraycup.MainMenu" />
        </activity>
        <activity
            android:name=".MainMenu"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_main_menu"
            android:parentActivityName=".LoadingScreen"
            android:theme="@style/FullscreenTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.CutlerDevelopment.murraycup.LoadingScreen" />
        </activity>
        <activity
            android:name=".PickATeamMenu"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_pick_ateam_menu"
            android:parentActivityName=".LoadingScreen"
            android:theme="@style/FullscreenTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.CutlerDevelopment.murraycup.LoadingScreen" />
        </activity>
        <activity
            android:name=".LoadingScreen"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>